# Test-only credentials. These are never used against a real endpoint — the
# tests point both clients at a local MockWebServer. This file shadows
# src/main/resources/config.properties on the test classpath.
anthropic_api_key=test-anthropic-key
oauth=test-github-token
